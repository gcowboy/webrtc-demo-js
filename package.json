{
  "name": "nuropad",
  "private": true,
  "workspaces": [
    "Backend",
    "Frontend",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@nuropad/shared-dto && npm run build --workspaces --if-present",
    "build:dto": "npm run build --workspace=@nuropad/shared-dto",
    "start:dev": "npm run build:dto && concurrently -n backend,frontend \"npm run start:dev:backend\" \"npm run start:dev:frontend\"",
    "start:dev:backend": "npm run start:dev --workspace=nuropad-backend",
    "start:dev:frontend": "npm run dev --workspace=nuropad-frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
